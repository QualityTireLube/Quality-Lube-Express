version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p deploy-output
        - cp -r * deploy-output/ || true
        - rm -rf deploy-output/deploy-output
  artifacts:
    baseDirectory: deploy-output
    files:
      - '**/*'
  cache:
    paths: []
